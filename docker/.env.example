# Copy to .env (same directory) and fill in. NEVER commit .env.
# Use the PAPER account credentials (the paper username IBKR generated
# when you enabled paper trading, usually a variant of your live username).
TWS_USERID=your_paper_username
TWS_PASSWORD=your_paper_password
TRADING_MODE=paper
